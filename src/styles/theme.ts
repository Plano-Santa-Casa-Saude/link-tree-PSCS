// Tema de cores da Santa Casa de Saúde SJC
export const theme = {
  colors: {
    // Cores principais
    primary: '#0a1929',        // Azul marinho escuro
    secondary: '#132f4c',      // Azul marinho médio
    accent: '#f8bd1c',         // Amarelo
    accentLight: '#ffd54f',    // Amarelo claro
    info: '#1976d2',           // Azul claro
    infoLight: '#42a5f5',      // Azul mais claro
    
    // Cores de fundo
    background: '#fafafa',     // Fundo claro
    surface: '#ffffff',        // Superfície branca
    sidebar: '#0a1929',        // Fundo da sidebar
    
    // Cores de texto
    textPrimary: '#ffffff',    // Texto principal (branco)
    textSecondary: '#e3f2fd',  // Texto secundário (azul muito claro)
    textAccent: '#f8bd1c',     // Texto de destaque (amarelo)
    
    // Cores de borda
    border: '#e0e0e0',         // Borda clara
    borderDark: '#132f4c',     // Borda escura
    
    // Cores de status
    success: '#4caf50',        // Verde para sucesso
    warning: '#ff9800',        // Laranja para aviso
    error: '#f44336',          // Vermelho para erro
    
    // Gradientes
    gradientPrimary: 'linear-gradient(135deg, #0a1929 0%, #132f4c 100%)',
    gradientSecondary: 'linear-gradient(180deg, #132f4c 0%, #0a1929 100%)',
    gradientAccent: 'linear-gradient(135deg, #f8bd1c 0%, #ffd54f 100%)',
  },
  
  // Sombras
  shadows: {
    small: '0 2px 8px rgba(10, 25, 41, 0.1)',
    medium: '0 4px 16px rgba(10, 25, 41, 0.15)',
    large: '0 8px 32px rgba(10, 25, 41, 0.2)',
    hover: '0 6px 20px rgba(10, 25, 41, 0.25)',
  },
  
  // Bordas arredondadas
  borderRadius: {
    small: '8px',
    medium: '12px',
    large: '16px',
  }
};
